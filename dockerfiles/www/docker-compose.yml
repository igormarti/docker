version: "2"
services:
  clinical:
    build:
        context: ./clinical
        dockerfile: dockerfile
    command: yarn start
    image: soft82/cliserver
    ports:
      - "3333:3333"
    restart: always
    volumes:
      - "./clinical:/usr/app"
    depends_on:
      - pg-database
  clinical-web:
    build:
        context: ./clinical-web
        dockerfile: dockerfile
    # command: npm start
    image: soft82/cliclient
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - "./clinical-web:/usr/src/app"
    depends_on:
      - pg-database
    stdin_open: true  
  pg-database:
    container_name: pg-database
    image: postgres:11
    restart: unless-stopped
    volumes:
      - ./:/data
    environment: 
      POSTGRES_DB: pgsql
      POSTGRES_USER: root
      POSTGRES_PASSWORD: rootpgsql       